# Hawaii Itinerary

A responsive Hawaii itinerary website connected live to the trip's Google Sheet. The public site reads only `Itinerary!A1:H7`, deliberately excluding the private Notes row.

To publish, open **Settings → Pages**, choose **Deploy from a branch**, select `main` and `/ (root)`, then save.
